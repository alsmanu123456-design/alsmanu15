{
  "name": "multi-browser-download",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m http.server 3000 --directory download",
    "start": "python3 -m http.server 3000 --directory download"
  }
}
